# UI Change Notes

Running list for small interface corrections collected during hands-on testing. These notes stay separate from the strategic roadmap.

## Applied

- `v1.32.1`: removed separate PIN glow/shadow effects; active PIN is brighter through fill color only.
- `v1.32.1`: corrected desktop field row heights so cells do not look flattened.
- `v1.32.2`: moved `SYNC/OK` beside `ACCOUNT`.
- `v1.32.2`: temporarily increased PIN labels to `16px`.
- `v1.32.3`: reduced PIN labels to `11px`.
- `v1.32.3`: moved the PIN switcher below the active work surface.
- `v1.32.4`: corrected the PIN switcher to sit between view tabs and the work surface.
- `v1.32.4`: returned PIN labels to normal `8px` text.

## Incoming

- Keep collecting quick UI notes here, then batch related changes instead of interrupting the main roadmap.
